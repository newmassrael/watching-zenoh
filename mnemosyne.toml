[workspace]
# README.md and docs/SESSION_KICKOFF.md removed per Round 7 scope correction
# (2026-05-08). README is a 0-byte stub (Phase E prose authoring deferred).
# SESSION_KICKOFF.md moved to notes/SESSION_KICKOFF.md as activity-log genre
# is a fit for ChangelogEntry, not Section. Round 1/5/6 atomic_impact_refs
# pointing at the two doc roots are absorbed by the workspace.docs
# membership change itself — removing the two paths from this list drops
# their cross-refs out of the orphan-eligible set, so no [[orphan_ledger]]
# entry is required. (Earlier draft of this comment referenced a Round 254
# atomic_entry_ref ledger entry "below"; that entry was never authored
# because the membership-level fix made it unnecessary.)
#
# Round 25 Phase E amendment (2026-05-15): README.md re-registered after
# R24 prose authoring + R25 English rewrite (parser-friendly form, no
# markdown URL links, no plain §N inline refs). Korean translation lives
# as README.ko.md satellite file outside workspace.docs governance per
# README language convention. The pre-existing atomic-store section
# `watching-zenoh` (R5 lean/stub form, Round 1/5/6/7 cited) remains as
# the doc-root anchor; atomic decompose of README H2 headings into typed
# Section fields deferred to a later round to keep R25 = register-only
# per CLAUDE.md "one phase per session, do not bundle".
docs = [
    "ARCHITECTURE.md",
    "README.md",
    "docs/intrinsics-runtime-symbols.md",
    "docs/oq-w15-ratification-summary.md",
    "docs/reassembly-fsm.md",
    "docs/rfc-open-questions-log.md",
    "docs/rfc-sce-protocol-synthesis.md",
    "docs/runtime-crate-lwip.md",
    "docs/runtime-crate-tokio.md",
    "docs/scouting-fsm.md",
    "docs/session-fsm.md",
    "docs/wire-spec-subset.md",
]
default_doc = "ARCHITECTURE.md"

[schema]
# rfc-open-questions-log.md uses `## Change log` (with space) for the
# session-by-session update log. Adding it lets the parser skip
# cross_ref extraction inside that section, eliminating the
# §change-log → §X orphan class.
changelog_titles = ["Changelog", "Change History", "Change log"]
# Default `Round ` prefix reserves the `Round N` pattern for future
# formal changelog entries authored via `append_changelog_entry_v2`.
# Existing date-based entries (`- **2026-04-24** — ...`) do not match
# this prefix and remain as prose in the section body.
entry_id_prefix = "Round "

[style]
locale = "en"
# Round 253 EXTERNAL-LEDGER adoption — legacy orphan carry (75 entries).
# Each row registers a known-stale §X.Y reference. Set-equality semantics:
# - new orphan added in source → fails validation until ledgered or fixed
# - entry resolved (ref fixed, target authored) → drift-catch reports it
# Triage is incremental; remove entries as refs resolve.

# Round 7 scope correction — README.md and docs/SESSION_KICKOFF.md
# removed from workspace.docs. Their root atomic sections (the
# `watching-zenoh` root and the legacy SESSION_KICKOFF root, whose
# atomic id is a KO slug locked since R5 — no `rename_section`
# primitive yet) remain in atomic store as historical record (typed-
# populated by Round 5 lean/stub). Round 1/5/6 impact_refs to those
# IDs are NOT dangling because the atomic_id_set retains them —
# atomic store outlives the markdown carrier doc, which is exactly
# the audit-trail genre's intent. No [[orphan_ledger]] entries are
# needed; Round 7 changelog entry alone documents the scope shift.

# Round 296 publishable_override_ledger — registers post-hoc
# publishable-half mutations so the audit/publishable divergence
# check accepts them. Each row pins a single changelog_entry's
# current publishable_hash with an audit-traceable reason.

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 121a"
reason = "Audit half kept minimal because the MCP append_changelog_entry_v2 call failed to deserialize the original rich payload (suspected arg-length / multi-byte handling at the publish boundary); publishable half populated via R295 set_changelog_publishable_* primitives to preserve the full R121a record."
applied_in = "R121a in-flight (sidecar override + re-append per CLAUDE.md anti-pattern #8 carve-out)"
content_hash_after = "dd94c34f95fb869d7cf41a32a96fe0a13e84eeeb0bbdbff20a42747f8f44e79b"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 122"
reason = "Audit half intentionally minimal per the R295/R296 textbook split: SCE upstream debt closures carry long narratives (cross-repo commit refs + before/after metrics + multi-round residual carry), so the audit half stays tight and the publishable half carries the full SCE pin bump + 71357264 wire-byte alignment + 4441431d overlay infra narrative."
applied_in = "R122 in-flight"
content_hash_after = "2b9a8b2c89c527f88750049a84e80f6dce0fb3232c304b2bd0b59f37f4b65ff0"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 123"
reason = "Audit half intentionally minimal per R295/R296 textbook split: this round is the deploy-yaml landing half of a 2-phase R123/R123b sequence (R123 = overlay schema in deploy yamls; R123b = wz-codecs build.rs consume + SCXML default removal). The audit half records the landing fact only; the publishable half carries the 3-deploy edit detail + R123b precondition list + R124-R126 Atomic B sequence."
applied_in = "R123 in-flight"
content_hash_after = "ebdc450f5626143d8c1ff54dd8c8028888dd09a58cf350325424e046d6264bd9"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 124"
reason = "Audit half intentionally minimal per R295/R296 textbook split: signal-only round (no wz code mutation), SCE-bound report body delivered to user for upstream relay. The audit half records the signal-drafted fact; the publishable half carries the zenoh-pico spec evidence trail (network.h flag definitions + network.c runtime is_local semantics) and the 3-option decision matrix (flag_defaults overlay vs runtime primitive vs status quo) that shapes the SCE response."
applied_in = "R124 in-flight"
content_hash_after = "66d577a7b6b96d3a4e99f6010f1d47c0809a28483ea373871e5f9f2a370637fc"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 125a"
reason = "Audit half intentionally minimal per R295/R296 textbook split: pin-bump-only round (the first sub-round of a 5-sub-round R125a-e adoption sequence for SCE B5-nu parent-tag variant codec dispatch). Audit half records the pin bump + regression-zero fact; publishable half carries the cumulative SCE Phase A + B context, the new diagnostic surface (codes 279-283), and the R125b-e carry plan including the wireexpr body-identical break risk + Layer 3 FFI compatibility verify."
applied_in = "R125a in-flight"
content_hash_after = "bd54f94159dfdb8f93e48e4868d29686b73d403f0205c260a27233af9a1260f1"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 125b"
reason = "Audit half intentionally minimal per R295/R296 textbook split: this round authors two new leaf codecs (wireexpr_local + wireexpr_nonlocal) under SCE B5-nu vendor pin b719ee3e. R125b's audit half originally claimed encoder-perspective arm assignment 0x00=wireexpr_local / 0x01=wireexpr_nonlocal — this was wrong (decoder-perspective reading of message.c:133's false-branch). R125c's empirical re-verification against network.c:42,116,250 (encoder OR-construction `_z_wireexpr_is_local(key) ? FLAG_M : 0`) + core.h:182 (`is_local` predicate) + SCE Phase B test fixture (`CodecB5nuKeyexprVariant::CodecB5nuLocal => 0x40u8`) confirms the production-correct assignment is 0x00=wireexpr_nonlocal (M=0 wire) and 0x01=wireexpr_local (M=1 wire) per encoder-side semantic. Publishable half retracts the inverted claim and records the corrected arm assignment, plus the R125c finding that wireexpr.scxml dispatcher integration is BLOCKED by three SCE upstream gaps (B5-gamma flag propagation through dispatcher; b5_nu_derivation_block alias-vs-stem mismatch; b5_nu_derivation_block Option<embed> handling)."
applied_in = "R125b in-flight (publishable retraction via R125c re-verification)"
content_hash_after = "6f955f8663ee27c4dd32f34af871e3da42f1c475df5e919e8b23595a4d6a71cd"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 125c"
reason = "Audit half intentionally minimal per R295/R296 textbook split: R125c is the SCE-upstream-signal half of the R125b-e adoption sequence. Integration attempt for wireexpr.scxml as a B5-nu parent-tag variant dispatcher surfaced three distinct SCE upstream gaps at vendor pin b719ee3e (B5-gamma flag propagation through the dispatcher to arms; b5_nu_derivation_block uses import alias for variant type name where it should use the dispatcher stem; b5_nu_derivation_block emits self.field.body for present-if-gated embeds without unwrapping the Option<Wireexpr>). All three gaps produced 19 cargo compile errors in the consumer codecs (decl_subscriber, decl_token, decl_queryable, push, request, response, interest_body) so the integration was rolled back; wireexpr.scxml remains the flat parent.N-RPF leaf shape and the six consumers keep their static <sce:flag name='M' value='1'/> baking (R106). R125b's audit half retains its original M-bit arm-assignment claim per frozen-ledger; R125b's publishable half (and the R125c entry) carry the corrected encoder-perspective reading. Publishable half of R125c records the three gap discoveries in full detail for the outgoing SCE upstream signal report, and itemizes the carry plan for resuming R125c-d after the SCE fixes land."
applied_in = "R125c in-flight"
content_hash_after = "875dd6c008442f5a6b18930d4a0c080b7a6c3711a89d533b01ca7230629917c3"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 126"
reason = "Audit half intentionally minimal per R295/R296 textbook split: pin-only round (R122/R125a precedent) advancing vendor/sce from b719ee3e to 11287248 after SCE upstream closed the three B5-nu consumer-parity gaps reported in R125c. Audit half records the pin bump fact + regression-zero verify + the three gap-close summaries; publishable half carries the full SCE atomic 11287248 anatomy (FlagLayoutSource enum at sce-build/src/forge/generator.rs:3380; embed_type_pascal cache removal and b5_nu_embed_pascal helper at generator.rs:9492; B5NuDerivation.present_if substrate at generator.rs:9313 with the 6-backend Some=>match / None=>0 expansion), the two new diagnostic codes 283->285, and the R125c-resume carry plan with the corrected encoder-side arm assignment locked from R125b's frozen-audit retraction. The R125c-resume entry will land under a new monotonic id (Round 125c1) per frozen-ledger semantics; the R125c entry itself records the signal/discovery half of the closure and the R126 row anchors the resolution-receipt half."
applied_in = "R126 in-flight"
content_hash_after = "30daf1927fd638fc949b44af999df77e888fe84ad3f52a84b52c84fb8385e835"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 125c1"
reason = "Audit half intentionally minimal per R295/R296 textbook split: R125c1 is the second SCE-upstream-signal half of the R125b-e adoption sequence. R125c-resume integration attempt on SCE vendor pin 11287248 (after R126's pin bump consumed atomic 11287248's three gap closures from R125c) surfaced three additional dispatcher-side codegen gaps that atomic 11287248 did not address: Gap 4 (dispatcher decode arm-dispatch uses undeclared `header` identifier where parent_flags is needed); Gap 5 (dispatcher encode arm-dispatch references nonexistent self.header field — same root cause as Gap 4, two emit sites); Gap 6 (consumer-site encode references the dispatcher's variant enum type without emitting the paired `use <dispatcher>::<DispatcherStem>Variant` import). All 12 wz-side changes were rolled back; HEAD remains at R126 a9212bc and the audit ledger documents the empirical findings for the second SCE upstream signal report. Publishable half carries the per-gap root-cause analysis with file+line excerpts from the generated wz-codecs out/*.rs files plus the carry plan for the second atomic, including the proposed SCE coverage fixture name (forge_b5_nu_dispatcher_with_rpf_arm_chain_compiles_rust) that would catch this class of bug at SCE's own test layer."
applied_in = "R125c1 in-flight"
content_hash_after = "091e950a09a53ada40d0a8fc822fa4d7338484e211eed436599b402d6e59cb51"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 127"
reason = "Audit half intentionally minimal per R295/R296 textbook split: pin-only round advancing vendor/sce from 11287248 to b35dbb66 after SCE upstream closed the three R125c1 dispatcher-self-gen gaps (Gap 4/5 dispatcher arm-dispatch parent_flags substitution via body_parent_flags_value_expr helper; Gap 6 Rust include_stmt brace-list augmentation for the variant enum import). Audit half records the pin bump fact + regression-zero verify + per-gap close summaries; publishable half carries the full SCE atomic b35dbb66 anatomy with file+line excerpts (helper at generator.rs:3383; three collapsed call sites; lib.rs:2760 brace-list patch; substring-assertion regression coverage; Q-DSG-3 rustc-compile harness deferral; Go backend package-qualifier cross-cutting carry) and the R125c2 adoption-path checklist."
applied_in = "R127 in-flight"
content_hash_after = "9a645e35279bb62d871355d3718efc054eb8d408098e2d35c647f2ef901a5be3"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 125c2"
reason = "Audit half intentionally minimal per R295/R296 textbook split: R125c2 is the production-land third attempt of the R125c integration sequence after SCE upstream's two atomics (11287248 + b35dbb66) closed all six dispatcher-side codegen gaps surfaced in R125c + R125c1. Audit half records the wireexpr.scxml restructure + 6 consumer M=1 drops + comment refresh + lib.rs/build.rs additions + workspace #[allow] for Gap 7 + pubsub.rs pattern-match update; publishable half carries the full per-component anatomy with cross-references to the SCE atomics' specific helper sites (FlagLayoutSource at generator.rs:3380; b5_nu_embed_pascal at 9492; B5NuDerivation.present_if at 9313; body_parent_flags_value_expr at 3383; include_stmt brace-list at lib.rs:2760-2774). Includes the cosmetic Gap 7 carry to SCE upstream (variant-only dispatcher emits dead let raw at decode prefix) suppressed via workspace-policy-permitted #[allow(unused_variables)] on pub mod wireexpr."
applied_in = "R125c2 in-flight"
content_hash_after = "18392b7fe3f6e68a330233d36b3fc9cc1d044994beb4e08c3ccacc31ff283335"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 128"
reason = "Audit half intentionally minimal per R295/R296 textbook split: pin-bump-plus-allow-drop round consuming the third SCE B5-ν follow-on cluster (atomics 2fe638c1 + 7bada841). Audit half records the pin bump + #[allow] removal + Gap 7 closure summary; publishable half carries the full per-atomic anatomy (Jinja2 {% if fields %} guard at all 6 backend templates; Python isinstance->kind discriminator fix at b5_nu_match_term; Go type-switch->field-presence fix at b5_nu_switch_block_go; Q-DSG-3 rustc-compile harness at tests/forge_conformance.rs::rustc_compile_codec_set with warnings=deny mirror; 6 new per-backend B5-nu fixtures), plus the R125c chain closure summary (7 gap classes resolved across 4 SCE atomics 11287248 -> b35dbb66 -> 2fe638c1 -> 7bada841) and the residual independent-priority carry list for R125e / R123b / R121b-c / R114 cluster."
applied_in = "R128 in-flight"
content_hash_after = "50a86f1d2cb769b385885b33a802b98604ec48dcabc67ad404111ffb14064c0e"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "Round 123b"
reason = "Audit half intentionally minimal per R295/R296 textbook split: R123b integration attempt deferred — SCE compile_scxml_with_imports (the only deploy-aware path covering both imports and the variant_defaults overlay) calls parse_deploy(deploy/ap_standalone.yaml) which rejects wz's pre-topology yaml schema (top-level `machines:` vs SCE-expected `topology.<device>.machines.<machine>` nesting at SCE pin 7bada841). All 3 working-tree changes (build.rs orchestrator switch + sources/codecs/request.scxml arm 0x03 default drop + scripts/run-ci.sh KNOWN_DIVERGENCE -= request) rolled back; HEAD stays at R128 dad20aa. Audit half records the deferral + the empirical schema mismatch; publishable half carries the full SCE-API survey (3 compile_* entry points considered), the rollback rationale, and the R123b-pre carry plan (yaml schema migration + validate-deploy.sh upgrade as workspace-wide refactor preconditions before R123b can resume)."
applied_in = "R123b in-flight"
content_hash_after = "763d7dc937411626613ff1a508e80d2c41da08feb71ab413d9c34f10327c52a5"

[[publishable_override_ledger]]
kind = "redaction"
target_id = "test"
reason = "Accidental minimal-content test entry appended during R121h-perf-bump-3 while bisecting an MCP append_changelog_entry_v2 deserialization failure. Root cause turned out to be a 200-char cap on decision_summary (Round 161 §41 MAX_INTENT_CHAR threshold) surfacing as a misleading 'missing field changes_bullets' parse-cascade error. Once the real R121h-perf-bump-3 entry was authored within the cap, append succeeded normally. The 'test' entry's audit half preserves the original test content (decision_summary='test summary', changes_bullets=[change1, change2], verification_bullets=[verify1]) as historical record of the bisection step; publishable half redacted via set_changelog_publishable_decision_summary / _changes / _verification to neutralize the spurious surface. Frozen-ledger gap accepted by recording the override here; no removal because Mnemosyne has no remove-entry primitive (frozen-ledger invariant)."
applied_in = "R121h-perf-bump-3 in-flight"
content_hash_after = "d9a919dfa7d92cdb6b86983e68bff3d2bad674e9398a3e019440c0f085a1d6a4"

# Round 193 atomic-internal orphan absorb — R121j-5c-e2e-action surfaced
# SessionLinkActions.send_response (tokio runtime trait surface change);
# the impact_refs citation should have pointed at the
# `runtime-crate--sce_link_runtime_tokio` atomic section, not RFC §66
# (the RFC sec numbering tops out at §16; §66 does not exist as a
# workspace section). Frozen-ledger preserves Round 193 body; the
# dangling atomic_entry_ref is absorbed via the Round 254 ledger
# mechanism so validate-workspace exits 0 with `new orphan = 0`.
[[orphan_ledger]]
kind = "atomic_entry_ref"
doc = "<atomic-changelog>"
from = "Round 193"
to = "66"
reason = "R121j-5c-e2e-action — Round 193 impact_refs §66 typo. Intended atomic section: runtime-crate--sce_link_runtime_tokio. RFC sec numbering tops out at §16; §66 is not a workspace section. R254 absorb."
since = "Round 193"
