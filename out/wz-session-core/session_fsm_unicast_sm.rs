// SCE-GENERATED — DO NOT EDIT
// source-hash: c36e29adfc747e86ea85756b63ebc03873d856936c9ec601c29dbdb44c63bd04
// template-hash: 90ac0b7250dd34a7e14136bc481cc93d6f1302dcf207c461738cfaee4b475c98
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Rust backend)
// From: sources/session/session_fsm_unicast.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-rust-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// SCE Protocol-Synthesis RFC §synth-5-J-2: no_std variant of the statechart
// emitter. `sce-rust-runtime` ships a `no_std` feature gate, and
// the generated crate omits `std::*` paths — Duration comes from
// `core::time`, the microstep dedup set is `heapless::FnvIndexSet`,
// and the parent-event queue field is elided (SCXML `<invoke>` is
// rejected at codegen time under `--no-std` per the
// `codegen/no-std-invoke-not-supported` diagnostic).


// Suppression budget audited 2026-04-15 against the full sce-rust-tests
// W3C suite (202 fixtures). Each allow below corresponds to a lint that
// actually fires on generated code today; see file:line callouts in the
// inline justifications. Adding a new allow here requires reproducing the
// warning on a real fixture and recording it in the same form. Removing an
// allow is safe whenever its justifying pattern stops being emitted.

// Source-faithful identifier names: W3C SCXML 6.4.1 lets users supply
// arbitrary id strings (e.g. `<invoke id="invokedChild">`); preserving the
// camelCase verbatim into Rust field names like `child_invokedChild`
// keeps the generated code traceable back to the SCXML source.
// Codegen always emits `use core::time::Duration` and the umbrella
// `use sce_rust_runtime::{Engine, StatePolicy};`; fixtures with no
// `<send delay=...>` and no engine-borrowing actions never reference
// `Duration`/`Engine`, leaving the imports unused.
// Method parameters such as `_engine: &mut Engine<Self>` and per-state
// fields (`pending_event_invokeid`, `script_engine_initialized`, ...) are
// emitted on every fixture for the StatePolicy trait shape but read only
// by fixtures that actually exercise the corresponding W3C feature.
// `let mut current_state = ...` in process_transition is mutated only by
// internal-transition fixtures; pure-external-transition fixtures leave it
// untouched after the let binding.
// `'action_block:` early-exit label wraps every onentry block but is only
// `break`-ed to from fixtures that emit error-on-action sequences.
// Codegen emits the catch-all `else => {}` arm on every closed-enum match
// for forward compatibility; rustc proves it unreachable once every
// variant is handled.
// W3C SCXML 5.7 donedata pattern: `let mut done_data_ok = true;` then
// per-param `done_data_ok = false;` on failure. Fixtures whose params
// always fail structurally (test298, test343 — empty `location=""`) never
// read the initial `true`.
// Generated code is uniform across 200+ fixtures and intentionally avoids
// per-fixture style optimisation passes. The `clippy::style` and
// `clippy::complexity` categories cover suggestions a human author would
// apply by hand (match_like_matches_macro, needless_range_loop,
// type_complexity, single_match, ...) — none of these affect correctness,
// but each one would force the codegen to special-case its output.
// Correctness-relevant clippy categories (`clippy::correctness`,
// `clippy::suspicious`, `clippy::perf`) remain at warn so any genuine bug
// the generator emits still surfaces.

// SCE-MAP: session_fsm_unicast.scxml:102 :: _machine

use core::time::Duration;
use sce_rust_runtime::{Engine, StatePolicy};


// ======================================================================
// State enum (W3C SCXML 3.3)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
pub enum SessionFsmUnicastState {
    Accepting,
    AwaitingInitSyn,
    Closed,
    Closing,
    Established,
    GotInitAck,
    // W3C SCXML 3.2: `<scxml initial>` state — the machine's `Default`.
    #[default]
    Init,
    LinkOpening,
    Opening,
    SentInitAck,
    SentInitSyn,
    SentOpenAck,
}

// ======================================================================
// Event enum (W3C SCXML 3.12)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum SessionFsmUnicastEvent {
    AcceptingInactivityTimeout,
    ClosingTimeout,
    EstablishmentExtRejected,
    FramingError,
    InboundStart,
    InitAckReceived,
    InitAckTimeout,
    InitSynReceived,
    LeaseExpired,
    LinkLost,
    LinkOpenTimeout,
    LinkOpened,
    OpenAckReceived,
    OpenAckTimeout,
    OpenSynReceived,
    OutboundStart,
    PeerClose,
    SessionClose,
    TxCongestionExhaust,
    /// W3C SCXML 3.13: Sentinel for eventless transition dispatch
    Null,
}

// ======================================================================
// Externally-drivable event surface (W3C SCXML 3.12 / 5.10)
// ======================================================================

impl SessionFsmUnicastEvent {
    /// Events that can arrive from OUTSIDE the machine — external
    /// `<send>` / platform ingress — as opposed to internal `<raise>`
    /// signals and the W3C SCXML 3.13 `Null` sentinel (neither is a
    /// member). This is the machine's structural external trigger
    /// surface: every non-reserved `<transition event>` target minus
    /// every internally-raised event. A consumer that maps event names
    /// back to variants admits only these as externally injectable,
    /// without re-deriving the partition. Structural and consumer-
    /// agnostic; inert when unused. Associated (not a free `const`) so
    /// several machines glob-re-exported into one module never collide
    /// on the name.
    pub const EXTERNALLY_DRIVABLE_EVENTS: &'static [SessionFsmUnicastEvent] = &[
        SessionFsmUnicastEvent::AcceptingInactivityTimeout,
        SessionFsmUnicastEvent::ClosingTimeout,
        SessionFsmUnicastEvent::EstablishmentExtRejected,
        SessionFsmUnicastEvent::FramingError,
        SessionFsmUnicastEvent::InboundStart,
        SessionFsmUnicastEvent::InitAckReceived,
        SessionFsmUnicastEvent::InitAckTimeout,
        SessionFsmUnicastEvent::InitSynReceived,
        SessionFsmUnicastEvent::LeaseExpired,
        SessionFsmUnicastEvent::LinkLost,
        SessionFsmUnicastEvent::LinkOpenTimeout,
        SessionFsmUnicastEvent::LinkOpened,
        SessionFsmUnicastEvent::OpenAckReceived,
        SessionFsmUnicastEvent::OpenAckTimeout,
        SessionFsmUnicastEvent::OpenSynReceived,
        SessionFsmUnicastEvent::OutboundStart,
        SessionFsmUnicastEvent::PeerClose,
        SessionFsmUnicastEvent::SessionClose,
        SessionFsmUnicastEvent::TxCongestionExhaust,
    ];
}

// ── W3C SCXML G.7: <sce:action> host-dispatch trait ──
/// W3C SCXML G.7: host operations dispatched by `<sce:action>`.
/// The generated `Policy` is generic over an implementation of this
/// trait; the host supplies the side effects while the statechart keeps
/// each operation symbolic. No runtime script engine is involved.
pub trait SessionFsmUnicastActions {
    fn enable_rx_tx_regions(&mut self);
    fn free_pool_slots(&mut self);
    fn link_driver_open(&mut self);
    fn record_established_at(&mut self);
    fn release_link(&mut self);
    fn send_close_frame_with_reason(&mut self);
    fn send_init_ack_with_cookie(&mut self);
    fn send_init_syn(&mut self);
    fn send_open_ack(&mut self);
    fn send_open_syn(&mut self);
    fn set_close_reason_expired(&mut self);
    fn set_close_reason_generic(&mut self);
    fn set_close_reason_invalid(&mut self);
    fn set_close_reason_unresponsive(&mut self);
    fn start_keepalive_worker(&mut self);
    fn start_lease_monitor(&mut self);
    fn stop_keepalive_worker(&mut self);
    fn stop_lease_monitor(&mut self);
}

// ======================================================================
// Policy struct
// ======================================================================

pub struct SessionFsmUnicastPolicy<A: SessionFsmUnicastActions + 'static> {
    // W3C SCXML G.7: host implementation of the `<sce:action>` operations.
    // The generated machine dispatches each native action through this field
    // (`self.actions.<op>(…)`) instead of a runtime script engine.
    actions: A,
    // W3C SCXML 3.13: Last transition metadata
    last_transition_is_internal: bool,
    last_transition_is_targetless: bool,
    last_transition_source_state: SessionFsmUnicastState,
    // W3C SCXML 3.13: Transition action tracking
    last_transition_index: usize,
    has_transition_actions: bool,
}

impl<A: SessionFsmUnicastActions + 'static> SessionFsmUnicastPolicy<A> {
    pub fn new(actions: A) -> Self {
        Self {
            actions,
            last_transition_is_internal: false,
            last_transition_is_targetless: false,
            last_transition_source_state: SessionFsmUnicastState::Init,
            last_transition_index: 0,
            has_transition_actions: false,
        }
    }




}


// ======================================================================
// StatePolicy trait implementation
// ======================================================================

impl<A: SessionFsmUnicastActions + 'static> StatePolicy for SessionFsmUnicastPolicy<A> {
    type State = SessionFsmUnicastState;
    type Event = SessionFsmUnicastEvent;
    // EventSchema native lowering: `()` = schemaless (dynamic
    // `_event.data` baseline); a `<Machine>Payload` sum is emitted when a
    // transition guard reads a typed `_event.data.<field>` (NL→IR C1 Path A).
    type Payload = ();
    // SCE Protocol-Synthesis RFC §synth-5-J-2: under no_std the std-backed `StdHal`
    // (`std::time::Instant` / `OnceLock`) does not implement `Hal`. The
    // generated machine defaults to `NoOpHal` so the no_std surface compiles
    // standalone. `NoOpHal` reports zero ticks and panics on `wake`/`irq_save`
    // (fail-loud per sce-rust-runtime hal docs) — an MCU consumer driving the
    // engine externally must provide a real `Hal` impl.
    type Hal = sce_rust_runtime::NoOpHal;

    // W3C SCXML Appendix D internalQueue / externalQueue. Under no_std the
    // depth is the per-document `<scxml sce:capacity>` / deploy value (emitted
    // as the const consumed below); absent a declared capacity the bare form
    // inherits the runtime default `MAX_EVENT_QUEUE_DEPTH` (the single source of
    // the default). Inert under std (`VecDeque`), so this one emission compiles
    // on both runtime profiles.
    type EventQueue = sce_rust_runtime::EventQueueManager<sce_rust_runtime::EventWithMetadata<Self::Event, Self::Payload>>;

    // W3C SCXML 6.3: the delayed-send scheduler keeps a per-entry `send_id` only
    // so `<cancel sendid>` can find and drop the matching entry; the timer-fire
    // drain never reads it. A document with no `<cancel>` therefore emits the
    // zero-size `ElidedSendId`, shedding the scheduler ring's per-entry
    // `heapless::String<256>` (~256 B x MAX_SCHEDULED_EVENTS) under no_std --
    // dead weight with no cancel reader. A cancelling document keeps the
    // load-bearing `SceString`. Behaviour-preserving under std either way, so
    // this one emission compiles on both runtime profiles. Mirrors the
    // EventQueue per-machine sizing lever above.
    type ScheduledSendId = sce_rust_runtime::ElidedSendId;

    // W3C SCXML feature flags
    const HAS_PARALLEL_STATES: bool = false;
    const NEEDS_SCRIPT_ENGINE: bool = false;
    const NEEDS_DATA_MODEL_INIT: bool = false;
    // The same fact the generate manifest publishes as `needs_event_scheduler`.
    // A `build.rs` consumer never sees that manifest — `compile_scxml` returns
    // `()` — so without this constant a host had no route to the knowledge that
    // its driving loop must call `tick()` rather than `step()`.
    const NEEDS_EVENT_SCHEDULER: bool = false;

    // ======================================================================
    // Static metadata methods (W3C SCXML document structure)
    // ======================================================================

    fn initial_state() -> Self::State {
        // The runtime entry state and the type's `Default` derive are one
        // concept, sourced from one computation (`default_state_id`) so
        // they can never diverge: `<scxml initial>`, or the first sorted
        // state when the initial attribute is unset.
        SessionFsmUnicastState::Init
    }

    fn is_final_state(state: Self::State) -> bool {
        match state {
            SessionFsmUnicastState::Closed => true,
            _ => false,
        }
    }

    fn get_parent(state: Self::State) -> Option<Self::State> {
        match state {
            SessionFsmUnicastState::AwaitingInitSyn => Some(SessionFsmUnicastState::Accepting),
            SessionFsmUnicastState::GotInitAck => Some(SessionFsmUnicastState::Opening),
            SessionFsmUnicastState::SentInitAck => Some(SessionFsmUnicastState::Accepting),
            SessionFsmUnicastState::SentInitSyn => Some(SessionFsmUnicastState::Opening),
            SessionFsmUnicastState::SentOpenAck => Some(SessionFsmUnicastState::Accepting),
            _ => None,
        }
    }

    fn is_compound_state(state: Self::State) -> bool {
        match state {
            SessionFsmUnicastState::Accepting => true,
            SessionFsmUnicastState::Opening => true,
            _ => false,
        }
    }


    fn is_descendant_of(desc: Self::State, anc: Self::State) -> bool {
        let mut current = desc;
        loop {
            match Self::get_parent(current) {
                None => return false,
                Some(parent) => {
                    if parent == anc {
                        return true;
                    }
                    current = parent;
                }
            }
        }
    }

    fn get_document_order(state: Self::State) -> u32 {
        match state {
            SessionFsmUnicastState::Accepting => 5,
            SessionFsmUnicastState::AwaitingInitSyn => 6,
            SessionFsmUnicastState::Closed => 11,
            SessionFsmUnicastState::Closing => 10,
            SessionFsmUnicastState::Established => 9,
            SessionFsmUnicastState::GotInitAck => 4,
            SessionFsmUnicastState::Init => 0,
            SessionFsmUnicastState::LinkOpening => 1,
            SessionFsmUnicastState::Opening => 2,
            SessionFsmUnicastState::SentInitAck => 7,
            SessionFsmUnicastState::SentInitSyn => 3,
            SessionFsmUnicastState::SentOpenAck => 8,
        }
    }

    fn get_event_name(event: Self::Event) -> &'static str {
        match event {
            SessionFsmUnicastEvent::AcceptingInactivityTimeout => "accepting.inactivity_timeout",
            SessionFsmUnicastEvent::ClosingTimeout => "closing.timeout",
            SessionFsmUnicastEvent::EstablishmentExtRejected => "establishment.ext_rejected",
            SessionFsmUnicastEvent::FramingError => "framing.error",
            SessionFsmUnicastEvent::InboundStart => "inbound.start",
            SessionFsmUnicastEvent::InitAckReceived => "init_ack.received",
            SessionFsmUnicastEvent::InitAckTimeout => "init_ack.timeout",
            SessionFsmUnicastEvent::InitSynReceived => "init_syn.received",
            SessionFsmUnicastEvent::LeaseExpired => "lease.expired",
            SessionFsmUnicastEvent::LinkLost => "link.lost",
            SessionFsmUnicastEvent::LinkOpenTimeout => "link.open_timeout",
            SessionFsmUnicastEvent::LinkOpened => "link.opened",
            SessionFsmUnicastEvent::OpenAckReceived => "open_ack.received",
            SessionFsmUnicastEvent::OpenAckTimeout => "open_ack.timeout",
            SessionFsmUnicastEvent::OpenSynReceived => "open_syn.received",
            SessionFsmUnicastEvent::OutboundStart => "outbound.start",
            SessionFsmUnicastEvent::PeerClose => "peer.close",
            SessionFsmUnicastEvent::SessionClose => "session.close",
            SessionFsmUnicastEvent::TxCongestionExhaust => "tx.congestion.exhaust",
            SessionFsmUnicastEvent::Null => "",
        }
    }

    fn get_event_from_name(name: &str) -> Option<Self::Event> {
        match name {
            "accepting.inactivity_timeout" => Some(SessionFsmUnicastEvent::AcceptingInactivityTimeout),
            "closing.timeout" => Some(SessionFsmUnicastEvent::ClosingTimeout),
            "establishment.ext_rejected" => Some(SessionFsmUnicastEvent::EstablishmentExtRejected),
            "framing.error" => Some(SessionFsmUnicastEvent::FramingError),
            "inbound.start" => Some(SessionFsmUnicastEvent::InboundStart),
            "init_ack.received" => Some(SessionFsmUnicastEvent::InitAckReceived),
            "init_ack.timeout" => Some(SessionFsmUnicastEvent::InitAckTimeout),
            "init_syn.received" => Some(SessionFsmUnicastEvent::InitSynReceived),
            "lease.expired" => Some(SessionFsmUnicastEvent::LeaseExpired),
            "link.lost" => Some(SessionFsmUnicastEvent::LinkLost),
            "link.open_timeout" => Some(SessionFsmUnicastEvent::LinkOpenTimeout),
            "link.opened" => Some(SessionFsmUnicastEvent::LinkOpened),
            "open_ack.received" => Some(SessionFsmUnicastEvent::OpenAckReceived),
            "open_ack.timeout" => Some(SessionFsmUnicastEvent::OpenAckTimeout),
            "open_syn.received" => Some(SessionFsmUnicastEvent::OpenSynReceived),
            "outbound.start" => Some(SessionFsmUnicastEvent::OutboundStart),
            "peer.close" => Some(SessionFsmUnicastEvent::PeerClose),
            "session.close" => Some(SessionFsmUnicastEvent::SessionClose),
            "tx.congestion.exhaust" => Some(SessionFsmUnicastEvent::TxCongestionExhaust),
            _ => None,
        }
    }

    fn get_state_name(state: Self::State) -> &'static str {
        match state {
            SessionFsmUnicastState::Accepting => "Accepting",
            SessionFsmUnicastState::AwaitingInitSyn => "AwaitingInitSyn",
            SessionFsmUnicastState::Closed => "Closed",
            SessionFsmUnicastState::Closing => "Closing",
            SessionFsmUnicastState::Established => "Established",
            SessionFsmUnicastState::GotInitAck => "GotInitAck",
            SessionFsmUnicastState::Init => "Init",
            SessionFsmUnicastState::LinkOpening => "LinkOpening",
            SessionFsmUnicastState::Opening => "Opening",
            SessionFsmUnicastState::SentInitAck => "SentInitAck",
            SessionFsmUnicastState::SentInitSyn => "SentInitSyn",
            SessionFsmUnicastState::SentOpenAck => "SentOpenAck",
        }
    }

    fn null_event() -> Self::Event {
        SessionFsmUnicastEvent::Null
    }

    // W3C SCXML 3.6: Get initial children of a compound state
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: return type is the runtime crate's
    // [`StateChain`] alias and the body uses `state_chain_from_slice` instead of
    // `vec![...]` so the emitted code compiles under `--no-std` (`vec!` is a
    // std-only macro; heapless has no equivalent).
    fn get_initial_children(state: Self::State) -> ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State> {
        match state {
            SessionFsmUnicastState::Accepting => ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([
                SessionFsmUnicastState::AwaitingInitSyn,
            ]),
            SessionFsmUnicastState::Opening => ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([
                SessionFsmUnicastState::SentInitSyn,
            ]),
            _ => ::sce_rust_runtime::helpers::hierarchy::new_chain(),
        }
    }

    // W3C SCXML 3.11: Get initial or history-restored child
    fn get_initial_or_history_child(&self, state: Self::State) -> Self::State {
        match state {
            SessionFsmUnicastState::Accepting => {
                SessionFsmUnicastState::AwaitingInitSyn
            }
            SessionFsmUnicastState::Opening => {
                SessionFsmUnicastState::SentInitSyn
            }
            _ => state,
        }
    }

    // ======================================================================
    // Mutable field accessors
    // ======================================================================

    fn last_transition_is_internal(&self) -> bool {
        self.last_transition_is_internal
    }

    fn set_last_transition_is_internal(&mut self, value: bool) {
        self.last_transition_is_internal = value;
    }

    fn last_transition_is_targetless(&self) -> bool {
        self.last_transition_is_targetless
    }

    fn set_last_transition_is_targetless(&mut self, value: bool) {
        self.last_transition_is_targetless = value;
    }

    fn last_transition_source_state(&self) -> Self::State {
        self.last_transition_source_state
    }

    fn set_last_transition_source_state(&mut self, state: Self::State) {
        self.last_transition_source_state = state;
    }




    // ======================================================================
    // Instance methods - generated executable content
    // ======================================================================



    // W3C SCXML 3.7: Execute <onentry> actions for a state
    #[doc = "SCE-MAP: session_fsm_unicast.scxml:102 :: _machine"]
// SCE-MAP: session_fsm_unicast.scxml:102 :: _machine
    fn execute_entry_actions(&mut self, state: Self::State, engine: &mut sce_rust_runtime::Engine<Self>, path_child: Option<Self::State>) {
        // Only a `<parallel>` machine descends into defaults here — see the
        // blocks at the end of this function — so a machine without one has
        // nothing to tell an ancestor entry from a target entry.
        let _ = path_child;
        match state {
            SessionFsmUnicastState::Closed => {
                // SCE-MAP: session_fsm_unicast.scxml:234 :: Closed :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="release_link">
            self.actions.release_link();

            // W3C SCXML G.7: <sce:action name="free_pool_slots">
            self.actions.free_pool_slots();
                }
            }
            SessionFsmUnicastState::Closing => {
                // SCE-MAP: session_fsm_unicast.scxml:226 :: Closing :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="send_close_frame_with_reason">
            self.actions.send_close_frame_with_reason();
                }
            }
            SessionFsmUnicastState::Established => {
                // SCE-MAP: session_fsm_unicast.scxml:199 :: Established :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="enable_rx_tx_regions">
            self.actions.enable_rx_tx_regions();

            // W3C SCXML G.7: <sce:action name="record_established_at">
            self.actions.record_established_at();

            // W3C SCXML G.7: <sce:action name="start_lease_monitor">
            self.actions.start_lease_monitor();

            // W3C SCXML G.7: <sce:action name="start_keepalive_worker">
            self.actions.start_keepalive_worker();
                }
            }
            SessionFsmUnicastState::GotInitAck => {
                // SCE-MAP: session_fsm_unicast.scxml:145 :: GotInitAck :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="send_open_syn">
            self.actions.send_open_syn();
                }
            }
            SessionFsmUnicastState::LinkOpening => {
                // SCE-MAP: session_fsm_unicast.scxml:118 :: LinkOpening :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="link_driver_open">
            self.actions.link_driver_open();
                }
            }
            SessionFsmUnicastState::SentInitAck => {
                // SCE-MAP: session_fsm_unicast.scxml:176 :: SentInitAck :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="send_init_ack_with_cookie">
            self.actions.send_init_ack_with_cookie();
                }
            }
            SessionFsmUnicastState::SentInitSyn => {
                // SCE-MAP: session_fsm_unicast.scxml:136 :: SentInitSyn :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="send_init_syn">
            self.actions.send_init_syn();
                }
            }
            SessionFsmUnicastState::SentOpenAck => {
                // SCE-MAP: session_fsm_unicast.scxml:182 :: SentOpenAck :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="send_open_ack">
            self.actions.send_open_ack();
                }
            }
            _ => {}
        }

    }

    // W3C SCXML 3.8: Execute <onexit> actions for a state
    #[doc = "SCE-MAP: session_fsm_unicast.scxml:102 :: _machine"]
// SCE-MAP: session_fsm_unicast.scxml:102 :: _machine
    fn execute_exit_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
        pre_transition_active: &[Self::State],
    ) {
        match state {
            SessionFsmUnicastState::Established => {
                // SCE-MAP: session_fsm_unicast.scxml:199 :: Established :: _state_body
                // W3C SCXML 3.9: onexit block 1/1
                // Labeled block allows actions to break out on error (W3C 3.9: error stops block)
                'action_block: {

            // W3C SCXML G.7: <sce:action name="stop_keepalive_worker">
            self.actions.stop_keepalive_worker();

            // W3C SCXML G.7: <sce:action name="stop_lease_monitor">
            self.actions.stop_lease_monitor();
                }
            }
            _ => {}
        }
    }



    // W3C SCXML 3.13: Evaluate guards and take a matching transition
    #[doc = "SCE-MAP: session_fsm_unicast.scxml:102 :: _machine"]
// SCE-MAP: session_fsm_unicast.scxml:102 :: _machine
    fn process_transition(
        &mut self,
        current_state: &mut Self::State,
        event: Self::Event,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> bool {
        let mut transition_taken = false;


        // W3C SCXML 3.12: Hierarchical event processing (innermost to outermost)
        let mut check_state = *current_state;

        loop {
            let found = self.try_transition_in_state(check_state, event, current_state, &mut transition_taken, engine);

            if found {
                break;
            }

            // W3C SCXML 3.13: Eventless transitions do NOT bubble to parent states
            if event == Self::null_event() {
                break;
            }

            // W3C SCXML 3.12: Move to parent state for hierarchical event bubbling
            match Self::get_parent(check_state) {
                Some(parent) => check_state = parent,
                None => break,
            }
        }

        transition_taken
    }

    // W3C SCXML 3.13: Execute transition actions (called between exit and entry)
    #[doc = "SCE-MAP: session_fsm_unicast.scxml:102 :: _machine"]
// SCE-MAP: session_fsm_unicast.scxml:102 :: _machine
    fn execute_transition_actions(&mut self, engine: &mut sce_rust_runtime::Engine<Self>) {
        if !self.has_transition_actions {
            return;
        }

        // Switch on source state, then transition index
        match self.last_transition_source_state {
            SessionFsmUnicastState::Accepting => {
                match self.last_transition_index {
                    1 => {
                        // SCE-MAP: session_fsm_unicast.scxml:189 :: Accepting :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_invalid">
            self.actions.set_close_reason_invalid();
                    }
                    2 => {
                        // SCE-MAP: session_fsm_unicast.scxml:192 :: Accepting :: _transition_2
                        // W3C SCXML 3.13: Transition 2 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_generic">
            self.actions.set_close_reason_generic();
                    }
                    _ => {}
                }
            }
            SessionFsmUnicastState::Established => {
                match self.last_transition_index {
                    0 => {
                        // SCE-MAP: session_fsm_unicast.scxml:210 :: Established :: _transition_0
                        // W3C SCXML 3.13: Transition 0 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_generic">
            self.actions.set_close_reason_generic();
                    }
                    1 => {
                        // SCE-MAP: session_fsm_unicast.scxml:213 :: Established :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_expired">
            self.actions.set_close_reason_expired();
                    }
                    2 => {
                        // SCE-MAP: session_fsm_unicast.scxml:216 :: Established :: _transition_2
                        // W3C SCXML 3.13: Transition 2 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_invalid">
            self.actions.set_close_reason_invalid();
                    }
                    3 => {
                        // SCE-MAP: session_fsm_unicast.scxml:219 :: Established :: _transition_3
                        // W3C SCXML 3.13: Transition 3 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_unresponsive">
            self.actions.set_close_reason_unresponsive();
                    }
                    _ => {}
                }
            }
            SessionFsmUnicastState::GotInitAck => {
                match self.last_transition_index {
                    1 => {
                        // SCE-MAP: session_fsm_unicast.scxml:150 :: GotInitAck :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_generic">
            self.actions.set_close_reason_generic();
                    }
                    _ => {}
                }
            }
            SessionFsmUnicastState::LinkOpening => {
                match self.last_transition_index {
                    1 => {
                        // SCE-MAP: session_fsm_unicast.scxml:123 :: LinkOpening :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_generic">
            self.actions.set_close_reason_generic();
                    }
                    _ => {}
                }
            }
            SessionFsmUnicastState::Opening => {
                match self.last_transition_index {
                    0 => {
                        // SCE-MAP: session_fsm_unicast.scxml:154 :: Opening :: _transition_0
                        // W3C SCXML 3.13: Transition 0 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_invalid">
            self.actions.set_close_reason_invalid();
                    }
                    1 => {
                        // SCE-MAP: session_fsm_unicast.scxml:157 :: Opening :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_generic">
            self.actions.set_close_reason_generic();
                    }
                    _ => {}
                }
            }
            SessionFsmUnicastState::SentInitSyn => {
                match self.last_transition_index {
                    1 => {
                        // SCE-MAP: session_fsm_unicast.scxml:141 :: SentInitSyn :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

            // W3C SCXML G.7: <sce:action name="set_close_reason_generic">
            self.actions.set_close_reason_generic();
                    }
                    _ => {}
                }
            }
            _ => {}
        }

        // Reset flags after execution
        self.has_transition_actions = false;
    }

}

// ======================================================================
// Helper impl block (try_transition_in_state, conflict resolution, etc.)
// ======================================================================

impl<A: SessionFsmUnicastActions + 'static> SessionFsmUnicastPolicy<A> {
    // W3C SCXML 3.12: Helper method for hierarchical transition checking
    fn try_transition_in_state(
        &mut self,
        check_state: SessionFsmUnicastState,
        event: SessionFsmUnicastEvent,
        current_state: &mut SessionFsmUnicastState,
        transition_taken: &mut bool,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> bool {
        match check_state {
            SessionFsmUnicastState::Accepting => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::AcceptingInactivityTimeout {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::FramingError {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::EstablishmentExtRejected {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 2;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::PeerClose {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 3;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LinkLost {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 4;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::AwaitingInitSyn => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::InitSynReceived {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::SentInitAck;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::Closed => false,
            SessionFsmUnicastState::Closing => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::ClosingTimeout {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LinkLost {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::Established => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::SessionClose {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LeaseExpired {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::FramingError {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 2;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::TxCongestionExhaust {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 3;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::PeerClose {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 4;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LinkLost {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 5;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::GotInitAck => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::OpenAckReceived {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Established;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::OpenAckTimeout {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::Init => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::OutboundStart {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::LinkOpening;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::InboundStart {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Accepting;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::LinkOpening => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LinkOpened {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Opening;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LinkOpenTimeout {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LinkLost {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 2;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::Opening => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::FramingError {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::EstablishmentExtRejected {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::PeerClose {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 2;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::LinkLost {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 3;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closed;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::SentInitAck => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::OpenSynReceived {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::SentOpenAck;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::SentInitSyn => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::InitAckReceived {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::GotInitAck;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == SessionFsmUnicastEvent::InitAckTimeout {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = SessionFsmUnicastState::Closing;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            SessionFsmUnicastState::SentOpenAck => {
                // W3C SCXML 3.13: Eventless transitions
                if event == SessionFsmUnicastEvent::Null {
                    // Unconditional eventless transition
                    self.last_transition_source_state = check_state;
                    self.last_transition_index = 0;
                    self.has_transition_actions = false;
                    self.last_transition_is_internal = false;
                    self.last_transition_is_targetless = false;
                        *current_state = SessionFsmUnicastState::Established;
                        *transition_taken = true;
                    return true;
                }
                false
            }
            _ => false,
        }
    }




}